# SUUMO 物件収集ツール (Streamlit版) 依存関係
# インストール:
#   python3 -m pip install --break-system-packages -r requirements_streamlit.txt
#
# 起動:
#   streamlit run app.py

# Streamlit 本体
streamlit>=1.30,<2.0

# 既存スクレイパー側の依存 (gui.py / test_run.py と共通)
requests>=2.31
beautifulsoup4>=4.12
lxml>=4.9

# Google Sheets 出力用
gspread>=5.12
google-auth>=2.23
